'use strict'

describe('Signup process', () => {

  beforeAll(async() => {
    return;
  });

  afterAll(() => {
    setTimeout(() => { process.exit() }, 1000);
  });

  test('Register User', () => {});

  test('Activate registration', () => {});

  test('Forgot password', () => {});

  test('Reset Password', () => {});

});
